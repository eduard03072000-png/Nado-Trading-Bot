# 🎯 QUICK START - Визуальная инструкция

## ✅ ЧТО УЖЕ ГОТОВО

```
telegram_trading_bot.py
├── ✅ States добавлены
├── ✅ show_positions() улучшена
├── ✅ set_tp_menu() добавлена
├── ✅ tp_mode_selected() добавлена
├── ✅ handle_tp_price() добавлена
├── ✅ handle_tp_percent() добавлена
└── ✅ confirm_tp_order() добавлена
```

## ⚠️ НУЖНО ДОДЕЛАТЬ (3 МЕСТА)

### 📍 МЕСТО 1: После tpsl_handler (~строка 1669)

```
┌─────────────────────────────────────┐
│ tpsl_handler = ConversationHandler │
│     entry_points=[...]              │
│     states={...}                    │
│     fallbacks=[...]                 │
│     per_message=False               │
│ )                                   │  ← ИЩЕМ ЭТУ СКОБКУ
└─────────────────────────────────────┘
                ↓
        ВСТАВИТЬ СЮДА
                ↓
┌─────────────────────────────────────┐
│ tp_handler = ConversationHandler   │  ← КОД ИЗ БЛОКА 1
│     entry_points=[...]              │
│     states={...}                    │
│     fallbacks=[...]                 │
│     per_message=False               │
│ )                                   │
└─────────────────────────────────────┘
```

### 📍 МЕСТО 2: После add_handler(tpsl_handler) (~строка 1698)

```
┌─────────────────────────────────────────┐
│ application.add_handler(tpsl_handler)  │  ← ИЩЕМ ЭТУ СТРОКУ
└─────────────────────────────────────────┘
                ↓
        ВСТАВИТЬ СЮДА
                ↓
┌─────────────────────────────────────────┐
│ application.add_handler(tp_handler)    │  ← КОД ИЗ БЛОКА 2
└─────────────────────────────────────────┘
```

### 📍 МЕСТО 3: После CallbackQueryHandler(close_position) (~строка 1714)

```
┌────────────────────────────────────────────────────────────────┐
│ application.add_handler(CallbackQueryHandler(close_position,  │  ← ИЩЕМ
│                         pattern=r'^close_\d+$'))               │     ЭТУ
└────────────────────────────────────────────────────────────────┘     СТРОКУ
                ↓
        ВСТАВИТЬ СЮДА
                ↓
┌────────────────────────────────────────────────────────────────┐
│ application.add_handler(CallbackQueryHandler(confirm_tp_order,│  ← КОД ИЗ
│                         pattern=r'^confirm_tp_'))              │    БЛОКА 3
└────────────────────────────────────────────────────────────────┘
```

## 🎯 ПРОВЕРКА

После вставки всех 3 блоков:

```python
def main():
    ...
    tpsl_handler = ConversationHandler(...)  # Уже было
    
    tp_handler = ConversationHandler(...)    # ✅ БЛОК 1 добавлен
    
    application.add_handler(tpsl_handler)    # Уже было
    application.add_handler(tp_handler)      # ✅ БЛОК 2 добавлен
    
    application.add_handler(CallbackQueryHandler(close_position, ...))  # Уже было
    application.add_handler(CallbackQueryHandler(confirm_tp_order, ...)) # ✅ БЛОК 3 добавлен
```

## 🚀 ТЕСТ

```bash
python telegram_trading_bot.py
```

В Telegram:
1. Открой позицию (LONG или SHORT)
2. Нажми "📊 Positions"
3. Должна появиться кнопка "🎯 Set TP"
4. Нажми на неё
5. Выбери режим (цена или процент)
6. Введи значение
7. Подтверди
8. TP ордер размещён на бирже! ✅

## 📊 РЕЗУЛЬТАТ

### Было:
```
📊 POSITIONS
BTC-PERP | Size: 0.01 | Price: $95,456
[❌ Close]
```

### Стало:
```
📊 OPEN POSITIONS

🟢 BTC-PERP
├ Size: 0.0100
├ Entry: $95,123.45    ← Цена входа
├ Current: $95,456.78  ← Текущая цена
├ Value: $954.57
└ P&L: 🟢 $3.33 (+0.35%)  ← Прибыль/убыток
   🎯 TP: $95,712.00    ← Take Profit

[🎯 Set TP]  ← Установить TP
[❌ Close]
```

## ❓ ВОПРОСЫ?

- Не работает? → Смотри `INTEGRATION_GUIDE.md`
- Ошибка? → Проверь `telegram_trading_bot_backup.py`
- Не понял? → Читай `DONE_STATUS.md`

Готово! 🎉
